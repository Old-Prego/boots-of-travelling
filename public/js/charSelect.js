// const { player_character } = require('../../models');

const startGame = async function() {

    document.location.replace('/gameplay/2');
      
};

// const popSidebar = async function(id) {
    
//     const characterData = await player_character.findByPk(id);

//     const character = characterData.get({ plain: true });

//     // set hp.innerHTML = character.hp
//     strength = character.strength;

// };

// find hp
// assign hp to variable